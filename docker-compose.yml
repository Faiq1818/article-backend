services:
  db:
    build: ./docker/development
    container_name: postgresql
    env_file:
      - .env
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - ./docker/development/init-db.sql:/docker-entrypoint-initdb.d/init.sql
      - pgdata:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    container_name: minio
    restart: unless-stopped
    ports:
      - "${MINIO_PORT}:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_API_CORS_ALLOW_ORIGIN: "*"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"

  # # monitoring image lgtm stack  
  # alloy:
  #   image: grafana/alloy:v1.7.5
  #   ports:
  #     - 12345:12345
  #     - 4317:4317
  #     - 4318:4318
  #   volumes:
  #     - ./config.alloy:/etc/alloy/config.alloy
  #     # - /var/run/docker.sock:/var/run/docker.sock:ro
  #     # - /var/lib/docker/containers:/var/lib/docker/containers:ro
  #
  #     # [DIUBAH] Mount Podman socket host ke lokasi yang diharapkan Alloy.
  #     # Asumsi menggunakan rootless Podman (UID 1000). Sesuaikan path jika menggunakan root Podman.
  #     - /run/user/1000/podman/podman.sock:/var/run/docker.sock:ro
  #
  #     # [DIUBAH] /var/lib/docker/containers dihapus. 
  #     # Diganti dengan mount journald karena Podman mengekspor log ke journald secara default.
  #     - /var/log/journal:/var/log/journal:ro
  #     - /run/log/journal:/run/log/journal:ro
  #     - /etc/machine-id:/etc/machine-id:ro
  #
  #   command: run --server.http.listen-addr=0.0.0.0:12345 --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy
  #   depends_on:
  #     - loki
  #   networks:
  #     - loki
  #

volumes:
  pgdata:
  minio_data:

# networks:
#   loki:
